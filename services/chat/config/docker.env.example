# Chat service runtime configuration (example values — DO NOT use in production)
DATABASE_URL=postgresql://chat_service:chat_service_password@postgres:5432/chat_service_db?sslmode=disable

JWT_ISSUER=ribble
# Replace with a strong, randomly generated secret (e.g. `openssl rand -base64 32`)
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET

FRONTEND_URL=http://localhost:3000
